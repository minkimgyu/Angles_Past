using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DamageComponent : MonoBehaviour


{
    //public void DamageToEntity(List<GameObject> gos, float damage, float knockBackThrust, List<EntityTag> entityTags)
    //{
    //    for (int i = 0; i < gos.Count; i++)
    //    {
    //        gos[i].TryGetComponent(out IHealth healthEntity);
    //        if (healthEntity == null) continue;

    //        for (int j = 0; j < entityTags.Count; j++)
    //        {
    //            if(healthEntity.IsTarget(entityTags[j])) healthEntity.UnderAttack(damage);
    //        }
    //    }
    //}

    //public void DamageToEntity(List<GameObject> gos, float damage, float knockBackThrust)
    //{
    //    for (int i = 0; i < gos.Count; i++)
    //    {
    //        gos[i].TryGetComponent(out IHealth healthEntity);
    //        if (healthEntity == null) continue;

    //        healthEntity.UnderAttack(damage);
    //    }
    //}
}